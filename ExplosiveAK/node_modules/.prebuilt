This folder exists so the PluginManager skips "npm install" — the plugin ships
prebuilt (see out/plugin.js). Delete this folder to force a reinstall/rebuild,
or run "npm run build" in the plugin folder after editing src/plugin.ts
(requires typescript).
